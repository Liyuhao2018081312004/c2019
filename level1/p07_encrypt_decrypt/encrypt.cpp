#include<bits\stdc++.h>
using namespace std;

int main()
{
	string s,o;
	cin >> s;
    int l = s.length();
	for(int i = 0;i < l; i++ ){
		o[i] = 256 - s[i];
		cout << o[i];
	}
    return 0;  
 } 
